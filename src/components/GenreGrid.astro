---
import type { Genre } from '../types';
import JanguIcon from './icons/JanguIcon.astro';
import BukIcon from './icons/BukIcon.astro';
import GayageumIcon from './icons/GayageumIcon.astro';
import HaegeumIcon from './icons/HaegeumIcon.astro';
import DansoIcon from './icons/DansoIcon.astro';
import GeomungoIcon from './icons/GeomungoIcon.astro';

interface Props {
  genres: Genre[];
  showHeading?: boolean;
}

const { genres, showHeading = true } = Astro.props;

const iconMap: Record<string, any> = {
  janggu: JanguIcon,
  buk: BukIcon,
  gayageum: GayageumIcon,
  haegeum: HaegeumIcon,
  danso: DansoIcon,
  geomungo: GeomungoIcon,
};
---

<section style="padding: 28px 24px 14px; max-width: 760px; margin: 0 auto;">
  {
    showHeading && (
      <div style="display: flex; align-items: baseline; justify-content: space-between; margin-bottom: 18px; padding-top: 10px; border-top: 0.5px solid var(--color-border);">
        <div>
          <span class="serif" style="font-size: 18px; font-weight: 500;">
            장르 / Genres
          </span>
          <span class="mono" style="font-size: 11px; color: var(--color-text-faded); margin-left: 12px;">
            {genres.length} categories
          </span>
        </div>
      </div>
    )
  }
  <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(160px, 1fr)); gap: 8px;">
    {
      genres.map((g) => {
        const Icon = iconMap[g.instrument] || JanguIcon;
        const isActive = g.status === 'active';
        return isActive ? (
          <a
            href={`/genre/${g.slug}`}
            class="genre-card-active"
            style="background: rgba(22, 18, 31, 0.6); backdrop-filter: blur(4px); border: 0.5px solid var(--color-border-strong); padding: 16px 14px; position: relative; display: block; transition: all 0.2s;"
          >
            <div style="margin-bottom: 10px; color: var(--color-text-primary);">
              <Icon size={28} />
            </div>
            <div class="serif" style="font-size: 16px; font-weight: 500; line-height: 1.2; color: var(--color-text-primary);">
              {g.instrument_label_ko}
            </div>
            <div class="mono" style="font-size: 11px; color: var(--color-text-faded); margin-top: 2px;">
              {g.label} · {g.track_count}
            </div>
            <div
              style="position: absolute; top: 12px; right: 12px; width: 6px; height: 6px; background: var(--color-dancheong-red); border-radius: 50%; box-shadow: 0 0 10px rgba(232, 72, 82, 0.7);"
            />
          </a>
        ) : (
          <div
            style="background: rgba(22, 18, 31, 0.4); border: 0.5px solid var(--color-border); padding: 16px 14px; opacity: 0.5;"
          >
            <div style="margin-bottom: 10px; color: var(--color-text-secondary);">
              <Icon size={28} />
            </div>
            <div class="serif" style="font-size: 16px; font-weight: 500; line-height: 1.2;">
              {g.instrument_label_ko}
            </div>
            <div class="mono" style="font-size: 11px; color: var(--color-text-faded); margin-top: 2px;">
              {g.label} · soon
            </div>
          </div>
        );
      })
    }
  </div>

  <style>
    .genre-card-active:hover {
      border-color: var(--color-dancheong-red);
      box-shadow: 0 0 24px rgba(232, 72, 82, 0.12);
    }
  </style>
</section>
