---
import Layout from '../layouts/Layout.astro';
import TrackHero from '../components/TrackHero.astro';
import GenreGrid from '../components/GenreGrid.astro';
import Stamp from '../components/Stamp.astro';
import tracksData from '../data/tracks.json';
import genresData from '../data/genres.json';
import type { Track, Genre } from '../types';

const tracks = tracksData as Track[];
const genres = genresData as Genre[];

const latest = [...tracks].sort((a, b) =>
  b.published_at.localeCompare(a.published_at),
)[0];
---

<Layout>
  {latest && <TrackHero track={latest} />}
  <GenreGrid genres={genres} />

  <footer
    style="max-width: 760px; margin: 48px auto 0; padding: 24px; display: flex; justify-content: space-between; align-items: center; border-top: 0.5px solid var(--color-border);"
  >
    <div class="mono" style="font-size: 10px; color: var(--color-text-faded); letter-spacing: 0.14em;">
      EST. 2026 · SEOUL · COSMOS
    </div>
    <Stamp />
  </footer>
</Layout>
